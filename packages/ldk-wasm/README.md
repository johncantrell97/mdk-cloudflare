# ldk-wasm

Generated workspace package for the `mdk-cloudflare` WASM bindings.

This directory is populated by `pnpm build:wasm`. It is bundled into the published
`mdk-cloudflare` package so consumers do not need a separate local path dependency.

Do not edit the generated `.js`, `.d.ts`, or `.wasm` files here by hand.
